import { selectPeers, useHMSStore, useHMSNotifications } from "@100mslive/react-sdk";

export default function Notifications() {
    const notifications = useHMSNotifications();
    console.log(notifications);

    return (
        <>
        
        </>
    );
}